import styled from 'styled-components'
import { Image } from 'react-bootstrap'

export const EmptyCart = styled(Image)`
  display: block;
  margin: 40px auto;
  max-width: 100%;
  height: auto;
`

export const MinPay = styled.h4`
  text-align: center;
  margin: 30px 0;
`